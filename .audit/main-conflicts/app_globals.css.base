:root {
  --navy: #17324d;
  --navy-dark: #0f2538;
  --blue: #2563eb;
  --blue-dark: #1d4ed8;
  --blue-soft: #dbeafe;
  --ink: #172033;
  --muted: #64748b;
  --line: #dbe3ed;
  --background: #f4f7fb;
  --card: #ffffff;
  --green: #166534;
  --green-soft: #dcfce7;
  --amber: #92400e;
  --amber-soft: #fef3c7;
  --red: #991b1b;
  --red-soft: #fee2e2;
  --radius: 16px;
  --shadow: 0 10px 30px rgba(15, 23, 42, 0.08);
}

* { box-sizing: border-box; }
html { background: var(--background); color: var(--ink); font-family: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; }
body { margin: 0; min-height: 100vh; }
a { color: inherit; }
button, input, select, textarea { font: inherit; }
button { cursor: pointer; }
button:disabled { cursor: not-allowed; opacity: .66; }
h1, h2, h3 { line-height: 1.15; }
p { line-height: 1.55; }
.sr-only { position: absolute; width: 1px; height: 1px; padding: 0; margin: -1px; overflow: hidden; clip: rect(0,0,0,0); white-space: nowrap; border: 0; }

.login-shell { min-height: 100vh; display: grid; grid-template-columns: 1.05fr .95fr; }
.login-hero { background: linear-gradient(145deg, #102a43, #1d4ed8); color: #fff; padding: 64px; display: flex; flex-direction: column; justify-content: center; }
.login-hero h1 { max-width: 700px; margin: 12px 0 20px; font-size: clamp(2.4rem, 5vw, 4.4rem); }
.login-hero p { max-width: 660px; color: #dbeafe; font-size: 1.08rem; }
.login-points { display: grid; gap: 12px; margin-top: 28px; }
.login-point { display: flex; gap: 12px; align-items: center; }
.login-point span:first-child { width: 28px; height: 28px; border-radius: 50%; display: grid; place-items: center; background: rgba(255,255,255,.14); font-weight: 900; }
.login-panel { display: grid; place-items: center; padding: 32px; }
.login-card { width: min(500px, 100%); background: #fff; border: 1px solid var(--line); border-radius: 24px; padding: 32px; box-shadow: var(--shadow); }
.eyebrow { color: var(--blue); font-size: .78rem; font-weight: 900; letter-spacing: .12em; text-transform: uppercase; }

.application-shell { min-height: 100vh; display: grid; grid-template-columns: 250px 1fr; }
.sidebar { position: sticky; top: 0; height: 100vh; overflow-y: auto; background: var(--navy); color: #fff; padding: 22px 16px; display: flex; flex-direction: column; }
.brand { display: flex; gap: 12px; align-items: center; padding: 3px 8px 24px; }
.brand-mark { width: 42px; height: 42px; border-radius: 13px; display: grid; place-items: center; background: var(--blue); font-size: 22px; }
.brand strong, .brand span { display: block; }
.brand span { color: #bfdbfe; font-size: .82rem; margin-top: 3px; }
.sidebar nav { display: grid; gap: 6px; }
.nav-link { color: #dbeafe; text-decoration: none; border-radius: 11px; padding: 12px 13px; font-weight: 760; }
.nav-link:hover, .nav-link:focus-visible { background: rgba(255,255,255,.12); color: #fff; }
.sidebar-account { margin-top: auto; border-top: 1px solid rgba(255,255,255,.16); padding: 18px 8px 0; }
.sidebar-account span, .sidebar-account small { display: block; color: #bfdbfe; }
.sidebar-account strong { display: block; margin: 4px 0; }
.sidebar-account form { margin-top: 14px; }
.main-content { min-width: 0; padding: 30px; max-width: 1500px; width: 100%; margin-inline: auto; }

.page-header { display: flex; justify-content: space-between; align-items: flex-start; gap: 18px; margin-bottom: 22px; }
.page-header h1 { margin: 0; font-size: 1.9rem; }
.page-header p { margin: 7px 0 0; color: var(--muted); }
.grid { display: grid; gap: 18px; }
.grid-2 { grid-template-columns: repeat(2, minmax(0, 1fr)); }
.grid-3 { grid-template-columns: repeat(3, minmax(0, 1fr)); }
.grid-4 { grid-template-columns: repeat(4, minmax(0, 1fr)); }
.card { background: var(--card); border: 1px solid var(--line); border-radius: var(--radius); box-shadow: 0 4px 18px rgba(15,23,42,.04); padding: 20px; min-width: 0; }
.card h2, .card h3 { margin-top: 0; }
.metric { display: flex; justify-content: space-between; gap: 14px; }
.metric-label { color: var(--muted); font-weight: 700; font-size: .9rem; }
.metric-value { margin-top: 8px; font-size: 2rem; font-weight: 950; }
.metric-icon { width: 44px; height: 44px; border-radius: 14px; display: grid; place-items: center; color: var(--blue-dark); background: var(--blue-soft); font-size: 20px; }
.field { display: grid; gap: 7px; margin-top: 16px; }
.field:first-child { margin-top: 0; }
label { font-size: .92rem; font-weight: 800; }
input, select, textarea { width: 100%; border: 1px solid #cbd5e1; background: #fff; color: var(--ink); border-radius: 12px; padding: 12px 14px; outline: none; }
input:focus, select:focus, textarea:focus { border-color: var(--blue); box-shadow: 0 0 0 3px rgba(37,99,235,.14); }
.form-grid { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 15px; }
.checkbox-row { display: flex; gap: 10px; align-items: flex-start; margin-top: 16px; }
.checkbox-row input { width: auto; margin-top: 3px; }
.button { min-height: 44px; border: 0; border-radius: 12px; padding: 11px 16px; font-weight: 850; display: inline-flex; align-items: center; justify-content: center; gap: 8px; text-decoration: none; }
.button-primary { background: var(--blue); color: #fff; }
.button-primary:hover { background: var(--blue-dark); }
.button-secondary { background: #fff; color: var(--ink); border: 1px solid var(--line); }
.button-danger { background: var(--red); color: #fff; }
.button-full { width: 100%; }
.button-small { min-height: 36px; padding: 7px 10px; font-size: .86rem; }
.actions { display: flex; flex-wrap: wrap; gap: 10px; align-items: center; }
.stack { display: grid; gap: 16px; }
.notice { border-radius: 12px; padding: 12px 14px; margin: 14px 0; font-weight: 700; }
.notice-success { background: var(--green-soft); color: var(--green); }
.notice-error { background: var(--red-soft); color: var(--red); }
.notice-info { background: #eff6ff; color: #1e40af; }
.table-wrap { overflow-x: auto; border: 1px solid var(--line); border-radius: 14px; }
table { width: 100%; min-width: 760px; border-collapse: collapse; }
th, td { text-align: left; padding: 13px 14px; border-bottom: 1px solid #edf2f7; vertical-align: middle; }
th { background: #f8fafc; color: #64748b; font-size: .78rem; text-transform: uppercase; letter-spacing: .05em; }
tr:last-child td { border-bottom: 0; }
.badge { display: inline-flex; align-items: center; border-radius: 999px; padding: 5px 9px; font-size: .76rem; font-weight: 900; }
.badge-success { background: var(--green-soft); color: var(--green); }
.badge-warning { background: var(--amber-soft); color: var(--amber); }
.badge-neutral { background: #eef2f7; color: #475569; }
.badge-danger { background: var(--red-soft); color: var(--red); }
.muted { color: var(--muted); }
.small { font-size: .86rem; }
.divider { border: 0; border-top: 1px solid var(--line); margin: 22px 0; }
.toolbar { display: flex; flex-wrap: wrap; gap: 10px; align-items: end; margin-bottom: 16px; }
.toolbar .grow { flex: 1; min-width: 230px; }
.empty { text-align: center; color: var(--muted); padding: 32px 18px; }
.service-banner { background: linear-gradient(135deg,#eff6ff,#eef2ff); border: 1px solid #bfdbfe; border-radius: var(--radius); padding: 20px; display: flex; justify-content: space-between; gap: 18px; align-items: center; margin-bottom: 18px; }
.service-banner h2 { margin: 5px 0; }
.danger-zone { border-color: #fecaca; }
.code-box { overflow: auto; background: #0f172a; color: #e2e8f0; padding: 14px; border-radius: 12px; font-family: ui-monospace, SFMono-Regular, Menlo, monospace; font-size: .86rem; }

@media (max-width: 1100px) {
  .grid-4 { grid-template-columns: repeat(2, 1fr); }
  .grid-3 { grid-template-columns: repeat(2, 1fr); }
}
@media (max-width: 820px) {
  .login-shell { grid-template-columns: 1fr; }
  .login-hero { padding: 40px 24px; }
  .login-panel { padding: 22px; }
  .application-shell { grid-template-columns: 1fr; }
  .sidebar { position: static; height: auto; }
  .sidebar nav { grid-template-columns: repeat(2, minmax(0, 1fr)); }
  .sidebar-account { margin-top: 22px; }
  .main-content { padding: 18px; }
  .grid-2, .grid-3, .grid-4, .form-grid { grid-template-columns: 1fr; }
  .page-header, .service-banner { flex-direction: column; align-items: stretch; }
}
@media (max-width: 520px) {
  .login-card { padding: 22px; }
  .sidebar nav { grid-template-columns: 1fr; }
  .actions .button { width: 100%; }
}
